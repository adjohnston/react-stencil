version: 2
jobs:
  build:
    working_directory: ~/tmp

    docker:
      - image: circleci/node:8.1.3

    steps:
      - checkout
      - run: npm install
      - run: npm test
      - run: npm build
